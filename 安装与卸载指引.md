# 每日打卡 - 安装与卸载指引

## 仓库结构说明

```
├── android-source/     安卓版完整源码（Gradle 项目）
├── windows-source/     Windows 版完整源码（Maven + JavaFX 项目）
├── releases/
│   ├── APK/           安卓安装包（app-debug.apk）
│   └── EXE/           Windows 可运行程序（JAR + 构建脚本）
└── 安装与卸载指引.md
```

---

## 一、Android 版

### 1. 安装

**方式一：直接安装 APK（推荐）**

1. 将手机连接电脑，或直接在手机上操作
2. 将 `releases/APK/app-debug.apk` 传到手机存储
3. 在手机文件管理器中找到该 APK 文件，点击安装
4. 如果提示"未知来源"，请在设置中允许安装未知来源应用
5. 安装完成后在桌面找到"每日打卡"图标，点击打开

**方式二：从源码编译**

1. 确保已安装 Android Studio（或 JDK 17 + Android SDK 34）
2. 用 Android Studio 打开 `android-source/` 目录
3. 等待 Gradle 同步完成
4. 连接手机（开启 USB 调试）或启动模拟器
5. 点击 Run 按钮即可安装运行

### 2. 卸载

**方式一：桌面卸载**
1. 在桌面长按"每日打卡"图标
2. 将图标拖动到"卸载"区域
3. 确认卸载

**方式二：设置卸载**
1. 打开 设置 → 应用管理
2. 找到"每日打卡"
3. 点击卸载

**数据清理**
卸载应用后，所有本地数据（任务、学习记录、消费记录、设置）将自动清除。

---

## 二、Windows 版

### 1. 安装

**方式一：直接运行 JAR（推荐）**

1. 确保已安装 Java 17 或更高版本
   - 下载地址：https://adoptium.net/
   - 安装后验证：打开命令行输入 `java -version`，确认显示 17 或更高
2. 将 `releases/EXE/dailycheck-windows.jar` 复制到任意目录
3. 双击 JAR 文件运行，或在命令行执行：
   ```
   java -jar dailycheck-windows.jar
   ```

**方式二：构建为 EXE 安装包**

1. 确保已安装：
   - JDK 17+（含 jpackage 工具）
   - Maven 3.8+
2. 将 `releases/EXE/build.sh` 复制到 Windows 源码目录
3. 打开命令行（CMD 或 PowerShell），进入 `windows-source/` 目录
4. 执行构建命令：
   ```
   mvn package
   ```
5. 构建完成后，在 `target/` 目录下会生成 `DailyCheck.exe` 安装包
6. 双击 `DailyCheck.exe` 按提示安装
7. 安装后从开始菜单找到"DailyCheck"启动

**方式三：从源码运行**

1. 确保已安装 JDK 17+ 和 Maven 3.8+
2. 进入 `windows-source/` 目录
3. 执行：
   ```
   mvn clean javafx:run
   ```

### 2. 卸载

**JAR 方式卸载**
1. 直接删除 `dailycheck-windows.jar` 文件
2. 删除数据文件：`%APPDATA%/dailycheck/` 目录（包含 SQLite 数据库和偏好设置）

**EXE 安装包卸载**
1. 打开 设置 → 应用 → 已安装的应用
2. 找到"DailyCheck"
3. 点击卸载
4. 删除残留数据：`%APPDATA%/dailycheck/` 目录

**数据清理**
- 数据库文件位置：`%APPDATA%/dailycheck/dailycheck.db`
- 偏好设置位置：注册表 `HKEY_CURRENT_USER\Software\com\dailycheck`
- 删除以上文件/目录即可完全清除所有数据

---

## 三、功能概览

| 功能 | Android | Windows |
|------|---------|---------|
| 任务打卡管理 | ✅ | ✅ |
| 学习计时器 | ✅ | ✅ |
| 收支记账 | ✅ | ✅ |
| 月历/周历/饼图统计 | ✅ | ✅ |
| 每日学习目标 | ✅ | ✅ |
| 每日消费上限 | ✅ | ✅ |
| 连续打卡统计 | ✅ | ✅ |
| 前台通知计时 | ✅ | - |
| 打卡提醒通知 | ✅ | - |

---

## 四、常见问题

**Q: Android 安装时提示"解析失败"？**
A: 请确认 Android 版本 >= 7.0 (API 24)，且存储空间充足。

**Q: Windows 运行 JAR 提示"找不到主类"？**
A: 请确认使用 Java 17+，执行 `java -jar dailycheck-windows.jar` 而非双击。

**Q: Windows 构建 EXE 失败？**
A: 确保 JDK 17+ 已安装且 `jpackage` 命令可用（`jpackage --version`）。

**Q: 数据丢失了怎么办？**
A: 数据存储在本地，卸载即清除。建议定期在设置页导出数据备份。
